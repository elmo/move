<%= render "shared/components/dropdown/dropdown",
           menu_position: "left-auto right-8 -top-4" do %>

  <%= render "shared/components/dropdown/dropdown_link",
             link_text: t('scaffold.view', resource: Provider, default: 'View %{resource}'),
             link_path: provider_path(provider),
             icon_name: "arrow-right" %>


  <%= render "shared/components/dropdown/dropdown_link",
               link_text: t('scaffold.edit', resource: Provider, default: 'Edit %{resource}'),
               link_path: edit_provider_path(provider),
               icon_name: "square-pen" %>


  <%= render "shared/components/dropdown/dropdown_link",
               link_text: t('scaffold.delete', resource: Provider, default: 'Delete %{resource}'),
               icon_name: "trash",
               link_path: provider_path(provider),
               link_method: :delete,
               button_link: true,
               data: {
                   turbo_method: :delete,
                   turbo_confirm: t('scaffold.really_delete', resource: provider, default: 'Really delete %{resource}?')
               } %>

<% end %>
